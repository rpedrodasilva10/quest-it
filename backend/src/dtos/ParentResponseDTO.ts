interface ParentResponseDTO {
  id: number;
  name: string;
  lastName: string;
  nickname?: string;
  createdAt: Date;
  updatedAt: Date;
  email: string;
  birthDate: String;
}

export default ParentResponseDTO;
